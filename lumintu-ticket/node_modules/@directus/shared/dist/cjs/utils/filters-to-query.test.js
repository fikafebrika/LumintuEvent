"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const _1 = require(".");
describe('filtersToQuery', () => {
    it('converts an array of filter fields to a filter object ', () => {
        const mockFilters = [{ _and: [{ field: 'field' }], _or: [{ filter: 'field' }] }];
        expect((0, _1.filtersToQuery)(mockFilters)).toStrictEqual({ filter: {} });
    });
});
